const OnboardingOne = () => {
  return (
    <section className={`my-6 text-center`}>
      <h1 className={`text-xl xs:text-2xl sm:text-4xl font-bold`}>
        🎉 Congratulations 🎉
      </h1>
      <h2 className={`text-sm xs:text-base sm:text-xl`}>
        Let's get your new landing page popping!
      </h2>
      <p className={`mt-16 text-sm sm:text-base text-action`}>
        (Usually takes about 5 minutes)
      </p>
    </section>
  );
};

export default OnboardingOne;

const SkipButton = () => {
  return (
    <button
      className={`py-0.5 border-b-[1px] border-transparent hover:border-stone-500 text-sm text-stone-500 transition-all duration-300`}
    >
      Skip Onboarding
    </button>
  );
};

export default SkipButton;

"use client";

import DarkModeToggle from "@/utils/DarkModeToggle";

const NavBar = () => {
  return (
    <nav className="w-full ">
      <DarkModeToggle />
    </nav>
  );
};

export default NavBar;
